# Данные

Датасет: **Default of Credit Card Clients** (UCI ML Repository, Тайвань, 30 000 клиентов).

Файлы данных не хранятся в репозитории. Чтобы скачать датасет, выполните из корня проекта:

```bash
python scripts/download_data.py
```

Появится файл `data/default_of_credit_card_clients.xls`.
